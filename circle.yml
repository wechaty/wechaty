machine:
  environment:
    XCODE_SCHEME: "test"
    XCODE_WORKSPACE: "platforms/ios"
    XCODE_PROJECT: "YourProject.xcodeproj"
  xcode:
    version: "7.3"
dependencies:
  override:
    - brew install node
    - npm install
test:
  override:
    - npm test